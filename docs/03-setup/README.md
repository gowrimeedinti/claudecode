# Section 3: Setup

_Getting Claude Code installed and configured for a Python/ML project._

## Topics to cover

- Prerequisites and installation (`npm install -g @anthropic-ai/claude-code`)
- Authentication (API key vs. Claude.ai subscription)
- IDE integration: VS Code extension, JetBrains
- Writing a useful `CLAUDE.md` for a Python repo
- `.claudeignore` — what to exclude (data files, secrets, large artifacts)
- Recommended settings for ML projects (large file handling, etc.)
